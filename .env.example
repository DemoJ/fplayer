PORT=3000
DATA_DIR=./data
# 注意：docker-compose.yml 中的 APP_SECRET 是硬编码的（作者自用、已在 GitHub 公开），
# 他人部署务必替换后再上线，详见 README 顶部警告。
APP_SECRET=replace-with-a-long-random-secret
FFMPEG_PATH=ffmpeg
FFPROBE_PATH=ffprobe
